//os module is a node.js module where you can write code about your operating system
// We need to import the os module to use the os module.
const os = require("os");

// Cheaking the operating system you are running on your computer :
console.log(os.platform());

console.log(os.freemem());
